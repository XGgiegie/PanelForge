# AGENTS.md

## 项目身份

这是一个 AI 漫剧 To C 产品。

核心目标：

通过 AI 技术，将小说内容自动转化为：
- 漫剧剧情
- 分镜
- 角色形象
- 图片/视频内容
- 配音
- 连续剧集

最终服务普通用户观看和消费。


---

# 产品核心原则

## 小说库是核心资产

整个系统围绕小说库展开。

核心链路：

小说库
→ 小说理解
→ 角色分析
→ 剧情拆解
→ 漫剧生成
→ 发布
→ 用户观看


所有新增功能必须优先考虑：

1. 是否增强小说价值？
2. 是否提升漫剧生成效率？
3. 是否提升用户体验？


不要开发脱离小说库的孤立功能。


---

# AI 分析后的产品架构

当前阶段优先把一个章节做出来。

对于长篇小说，不优先做全书改编规划或前几集规划。

原因：

- 长篇内容跨度大，过早规划全书容易失真
- 普通用户更需要先看到一个章节能不能被做成漫剧
- 单章样片能更快验证角色、画风、分镜和生成链路

AI 分析之后，产品必须继续引导用户完成：

小说库
→ 创作大纲
→ 选择章节
→ 章节 AI 分析
→ 本章分镜
→ 分镜画布
→ 图片 / 视频生成
→ 配音
→ 本章预览


## 剧集与分镜

当前分镜画布优先服务单个章节。

每个分镜卡片包含：

- 画面描述
- 角色
- 台词
- 镜头类型
- 生成图片入口

优先目标：

- 让普通用户快速看到单章样片
- 让 AI 生成链路连续推进
- 让内容资产能沉淀到小说下
- 后续再把多个章节串成连续剧集


## 分镜画布决策

当前阶段可以做轻量单章分镜画布。

定位：

把“AI 分析 + 正文”生成的分镜，以文本卡片形式放到一个独立窗口中。

画布只服务当前章节：

- 展示每个分镜文本
- 在分镜卡片上生成图片
- 在分镜卡片上生成视频
- 让用户快速看到单章制作链路

不要把画布做成复杂 To B 编辑器。

暂不做：

- 多章节全局编排
- 复杂拖拽连线
- 资产依赖图
- 专业时间线编辑器
- 复杂导演模式


---

# 开发原则

## 简单优先

这是一个商业化 To C 产品。

优先选择：

- 简单方案
- 快速迭代
- 易维护架构
- 用户能感知的功能


避免：

- 复杂架构
- 过度设计
- 技术炫技
- 无用户价值功能


---

# Codex 行为规范

## 修改代码前

必须：

1. 理解当前代码结构
2. 找到相关模块
3. 确认影响范围


禁止：

- 随意重构
- 删除已有功能
- 修改无关模块
- 创建重复功能


---

## 修改代码原则

优先：

- 小范围修改
- 保持现有架构
- 复用已有组件
- 保持代码一致性


如果需要大规模调整：

必须先说明：
- 原因
- 影响范围
- 为什么需要这样做


---

# UI / 产品体验规范

这是 C 端产品，不是后台管理系统。

这是正式产品，不是功能演示或原型。每个页面和功能都必须有明确的用户目标、主操作和下一步去向；不能只展示数据或做出一个可点击的界面。

## 产品逻辑优先

开发新功能前，先明确：

1. 用户从哪里进入
2. 用户在当前页面要完成什么
3. 主操作完成后，结果保存在哪里、如何继续使用
4. 用户失败、取消、重复操作或对结果不满意时如何处理

必须：

- 围绕完整任务链路设计，不做孤立的“入口 + 表单 + 提交”页面
- 将生成结果、上传内容和用户选择沉淀为可复用资产
- 为异步操作提供进行中、成功、失败、空状态和重试路径
- 对可替换的生成结果保留历史版本，用户主动选择当前使用版本
- 让关键结果在后续分镜、图片、视频等环节可见、可使用、可追溯

禁止：

- 用仅存在于当前页面内存中的状态代替应保存的业务数据
- 自动覆盖用户已选择的结果
- 只处理生成成功的理想路径而忽略失败和不满意结果
- 以“以后再补”为理由交付断裂的核心流程


设计原则：

- 少文字
- 强视觉
- 强引导
- 操作简单


用户应该感觉：

"选择一本小说，AI帮我制作漫剧"


而不是：

"我正在配置一个复杂AI系统"


避免：

- 大量标题
- 技术参数展示
- 复杂表单
- 过多说明文字


## 视觉质量

页面必须具备成熟消费产品的视觉层级和完成度，不接受默认组件堆叠、后台式信息罗列或临时 Demo 布局。

要求：

- 先确定页面主视觉、主内容和主操作，再安排次级信息
- 图片、角色、分镜等内容资产应成为视觉重点，信息区为其服务
- 使用稳定的栅格、比例和间距；图片、卡片、工具栏和抽屉不得因内容变化跳动
- 操作层级清晰：每个场景只有一个明确的主操作，次级操作收纳到合适位置
- 用抽屉、弹窗、预览和历史记录承接深入操作，避免把所有内容同时堆在主页面
- 保持克制的色彩、边框、阴影和动效，优先清晰、精致、耐看
- 文字、按钮、图片和状态在桌面及移动端均不得溢出、遮挡、错位或难以点击

避免：

- 无业务含义的大色块、渐变装饰、空白占位和装饰性卡片
- 同一页面出现多个抢占注意力的主按钮
- 关键操作隐藏在仅靠颜色或悬停才能理解的交互中
- 将配置参数直接作为页面主体暴露给普通用户


---

# AI功能规范

AI能力必须考虑：

## 稳定性

生成结果需要：

- 可控制
- 可追踪
- 可重复优化


## Prompt管理

所有重要 Prompt：

需要：

- 集中管理
- 可版本控制
- 可调整


不要：

- 将 Prompt 随意散落代码中


---

# 数据设计原则

核心数据：

小说 Novel

章节 Chapter

角色 Character

剧情 Plot

分镜 Scene

图片 Image

视频 Video

声音 Voice

剧集 Episode


数据关系：

Novel
 ↓
Chapter
 ↓
Plot
 ↓
Scene
 ↓
Episode


设计数据库时优先考虑：

- 扩展性
- AI生成流程
- 内容资产沉淀


---

# 前端规范

## 项目内 Skill

本项目将 Naive UI 开发规范版本化在：

`.codex/skills/naive-ui/`

涉及 Naive UI 组件、表单、布局、弹窗或主题时，必须先阅读：

`.codex/skills/naive-ui/SKILL.md`

再按任务读取对应的 `references/` 文档。项目内 Skill 是团队协作的基准，不能只依赖开发机上的全局 Skill。

## 组件选择检查

每次开始前端开发或重构前，必须先完成组件映射，再写模板：

1. 列出页面需要的布局、列表、表单、图片、反馈和覆盖层
2. 在 Naive UI 及本项目 Skill 中确认每项对应组件
3. 优先组合现有组件完成界面，仅为业务内容、间距和响应式关系补充布局样式

默认映射：

- 页面框架、侧栏、页头、内容区和页脚使用 `NLayout`、`NLayoutHeader`、`NLayoutSider`、`NLayoutContent`、`NLayoutFooter`
- 列表使用 `NList`、`NListItem`、`NThing`
- 表单使用 `NForm`、`NFormItem`、`NFormItemGi`、`NGrid`
- 图片使用 `NImage`、`NImageGroup`，并保留预览能力
- 操作、状态、反馈和覆盖层使用 `NButton`、`NTag`、`NAlert`、`NEmpty`、`NDrawer`、`NModal`

只有组件库确实没有对应能力时，才允许使用普通元素承担纯业务内容或布局；不得用 `div`、`button`、`input` 等重复实现组件库已有的卡片、按钮、输入框、列表、图片预览、抽屉、弹窗或反馈组件。

目标：

接近成熟互联网产品。


## 组件复用

项目已使用 Naive UI。

开发页面时必须：

- 优先使用 Naive UI 已有组件
- 卡片使用 `NCard`
- 按钮使用 `NButton`
- 输入框使用 `NInput`
- 对话框、提示、空状态等优先使用对应的 Naive UI 组件
- 优先采用 Naive UI 默认样式，仅为业务布局补充少量样式

禁止：

- 重复实现组件库已经提供的通用组件
- 使用普通 `div` 模拟卡片、按钮、输入框等基础组件
- 直接覆盖 `.n-*` 内部类来重写 Naive UI 外观
- 无明确产品价值的组件封装和视觉特效


要求：

- 页面简洁
- 操作路径短
- 用户无需学习
- 页面首次进入即可理解当前内容、当前状态和下一步
- 关键生成任务必须能看到任务状态、结果、失败原因和后续操作
- 图像类功能必须使用预览组件，并支持查看与选择已保存结果


避免：

- 工程后台风格
- 大量表格
- 技术信息暴露


---

# 后端规范

API设计：

保持业务语义。


推荐：

GET /novels

GET /novels/{id}

POST /dramas/generate

GET /episodes/{id}


避免：

复杂、不清晰的接口命名。


---

# 验证规范

默认使用快速验证模式。


修改后：

执行：

1. 代码检查
2. 相关模块测试
3. 必要验证


## UI 改动验收

涉及页面结构、视觉、交互或响应式布局时，除代码检查外必须：

1. 启动本地服务并实际打开目标页面
2. 分别检查桌面和移动端视图
3. 验证主操作、加载、空状态、失败状态及已保存数据的回显
4. 检查图片比例、文字换行、滚动区域、弹窗/抽屉层级和点击区域

未完成以上验收，不得仅以“编译通过”作为 UI 完成标准。


不要默认执行：

- 全量测试
- 长时间构建
- 无关检查


如果验证预计超过3分钟：

先停止并说明情况。


---

# 输出规范

回复保持简洁。


默认格式：

完成：
- 修改内容
- 文件变化
- 验证结果


不要输出：

- 大量背景介绍
- 无关解释
- 重复内容


---

# 需求不明确时

优先：

1. 根据 AI 漫剧产品方向推断
2. 选择简单方案
3. 保留未来扩展能力


不要：

自行扩大需求范围。


---

# 长期目标

持续建设：

一个基于小说库的 AI 漫剧生产平台。

所有代码和功能：

必须服务于：

小说资产积累
AI内容生产
用户消费体验
商业化增长
